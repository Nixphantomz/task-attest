# Rename to `.env.local` for local dev, or set these in Vercel's project env.

# The deployed TaskAttest address (from deploy/deploy.js output).
# NEXT_PUBLIC_ prefix = exposed to the browser (safe: it's a public address).
NEXT_PUBLIC_CONTRACT_ADDRESS=

# Optional: WalletConnect project id (for mobile QR connect).
# Free at https://cloud.walletconnect.com. Browser-extension wallets
# (OKX, MetaMask) work without it.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# --- IPFS pinning for the /api/pin route (SERVER-SIDE secrets, no NEXT_PUBLIC_) ---
# Option A: Pinata (recommended, simplest). Create a JWT in the Pinata dashboard.
PINATA_JWT=

# Option B: any generic bearer-token JSON pin endpoint.
# PIN_ENDPOINT=
# PIN_TOKEN=
