# Rename this file to `.env` (in the deploy/ folder) and fill in the blanks.
# `.env` must never be committed — it holds your private key.

# --- X Layer Testnet (Terigon, chain ID 1952) ---
XLAYER_TESTNET_RPC=https://testrpc.xlayer.tech/terigon
# Alternative RPC if the above is flaky:
# XLAYER_TESTNET_RPC=https://xlayertestrpc.okx.com/terigon

# The wallet that deploys the contract and becomes admin (DEFAULT_ADMIN_ROLE).
# Fill this in your LOCAL .env only. NEVER commit a real private key.
DEPLOYER_PRIVATE_KEY=

# Circle-issued native USDC on X Layer Testnet (confirmed via Circle docs).
# This is the token the escrow will custody. Do NOT deploy MockUSDT anymore.
USDT_ADDRESS=0xDec90b78111Ba2fc6FC6d84d8B9ec159A2d4b9B3

# Filled in AFTER deploy.js prints the deployed address.
# Needed by grantRole.js and createTask.js.
CONTRACT_ADDRESS=

# --- optional: expected chain id, used as a safety check ---
EXPECTED_CHAIN_ID=1952
